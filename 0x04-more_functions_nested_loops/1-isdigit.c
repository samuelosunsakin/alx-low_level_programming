#include "main.h"

/**
 * _isdigit - Function that check for a digit
 * (0 through 9)
 * @c: Parameter that checks
 * Return: Returns 1 otherwise 0
 */

int _isdigit(int c)
{
	return ((c >= 48) && (c <= 57));
}
